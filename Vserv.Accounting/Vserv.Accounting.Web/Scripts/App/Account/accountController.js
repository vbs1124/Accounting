(function () {
    'use strict';

    window.app.controller('AccountController', loginController);

    loginController.$inject = ['$window', '$http'];
    function loginController($window, $http) {
 
    }
})();
